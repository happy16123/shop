<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.myshop.mapper.BoardMapper">
	
	<select id="allList" resultType="com.myshop.domain.BoardVO">
		<![CDATA[
			select bno, title, content, writer, regdate, updatedate, replycnt
			from tbl_board
			where bno > 0
		]]>
	</select>
	
	<select id="detail" resultType="com.myshop.domail.BoardVO">
		select bno, title, content, writer, regdate, updatedate, replycnt
		from tbl_board
		where bno=#{bno}
	</select>
	
	<insert id="insert">
		insert tbl_board into(bno, title, content, writer)
			value (seq_board.nextval, #{title}, #{content}, #{writer})
	</insert>
	
	<delete id="delete">
		delete from tbl_board where bno=#{bno} 
	</delete>
	
	<update id="update">
		update tbl_board
			set title=#{title}, content=#{content}, updateDate=sysdate
			where bno=#{bno}
	</update>
	
</mapper>